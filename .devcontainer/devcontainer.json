{
  "name": "ClubManager Dev",
  "image": "php:8.3-cli",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y unzip git curl libpq-dev && curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer && docker-php-ext-install pdo_pgsql"
}


